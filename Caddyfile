{
    servers {
        trusted_proxies static private_ranges
    }
}

{$CADDY_ADDR:-:80} {
    handle /graphql {
        reverse_proxy api:8080
    }

    handle /health {
        reverse_proxy api:8080
    }

    handle {
        reverse_proxy web:3000
    }
}
