{$CADDY_ADDR:-:80} {
    trusted_proxies private_ranges

    handle /graphql {
        reverse_proxy api:8080
    }

    handle /health {
        reverse_proxy api:8080
    }

    handle {
        reverse_proxy web:3000
    }
}
