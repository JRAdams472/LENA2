import type { RequestHandler } from "msw";

// Add request handlers here as new tests are written.
// Example:
// import { graphql } from "msw";
// export const handlers: RequestHandler[] = [
//   graphql.query("GetItems", () => HttpResponse.json({ data: { items: [] } })),
// ];

export const handlers: RequestHandler[] = [];
